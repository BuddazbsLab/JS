let admin = 'Alex Stone'; 	// Передаем в переменную admin строковое значение 'Alex Stone';
let name = admin; 		   // Присваиваем переменной name значение переменной admin; 
console.log(name);		  // Вывод результата в консоль;